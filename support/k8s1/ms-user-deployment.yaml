apiVersion: v1
kind: Service
metadata:
  labels:
    app: ms-user
  name: ms-user
spec:
  ports:
  - port: 8080
    name: tcp
    protocol: TCP
    targetPort: 8080
  selector:
    app: ms-user
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: ms-user
  name: ms-user
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-user
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: ms-user
    spec:
      containers:
      - image: b193/ms-user:latest
        name: ms-user
        ports:
        - containerPort: 8080
        # env:
        #   - name: NODE_NAME
        #     valueFrom:
        #       fieldRef:
        #         fieldPath: spec.nodeName
        #   - name: POD_NAME
        #     valueFrom:
        #       fieldRef:
        #         fieldPath: metadata.name
        #   - name: POD_NAMESPACE
        #     valueFrom:
        #       fieldRef:
        #         fieldPath: metadata.namespace
        #   - name: POD_IP
        #     valueFrom:
        #       fieldRef:
        #         fieldPath: status.podIP
        # resources: {}
